const {makeAnagram} = require('../HackerRank/MakeAnagram');

describe.only('Make Anagrams', () => {
    it('', () => {
        expect(makeAnagram('cde', 'abc')).toBe(4);
    })
    it('', () => {
        expect(makeAnagram('a', 'aaaa')).toBe(3);
    })
    it('', () => {
        
    })
})